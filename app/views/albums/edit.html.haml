= form_for(@album) do |f|
  %fieldset
    %legend Editing Album
    = render partial: 'form', locals: {f: f}
    .form-actions
      = f.submit 'Save', class: 'btn btn-primary'

= link_to 'Show', @album
