.header-actions
  = link_to('Edit', edit_album_path(@album), class: 'btn btn-primary btn-mini')
  = link_to('Delete', @album, confirm: 'Are you sure?', method: :delete, class: 'btn btn-danger btn-mini')